{
  "name": "oficina-pro-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/admin-web",
    "apps/client-portal"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
