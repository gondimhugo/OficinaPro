# ------------------------------------------------------------
# Variáveis consumidas pelo `docker-compose.yml` via interpolação
# (`${VAR:-default}`). Copie para `.env` na raiz do monorepo.
#
# Para rodar a API fora do Docker, veja também `apps/api/.env.example`.
# ------------------------------------------------------------

# Conexão Supabase (direct connection na porta 5432).
# Substitua <DB_PASSWORD> pela senha do Postgres obtida em
# Supabase > Project Settings > Database.
DATABASE_URL=postgresql://postgres:<DB_PASSWORD>@db.paditvetvgtyriooiabs.supabase.co:5432/postgres

# Gere um segredo forte em produção: `openssl rand -hex 32`
JWT_SECRET_KEY=change-me-in-production-at-least-32-bytes-long

SUPABASE_URL=https://paditvetvgtyriooiabs.supabase.co
SUPABASE_ANON_KEY=sb_publishable_dp34TL3Xlofe92LqQr8DpA_VdsCk_Ti
SUPABASE_SERVICE_ROLE_KEY=
